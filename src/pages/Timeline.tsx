// STUB — to be replaced with the ported page.
export default function TimelinePage(props: Record<string, unknown>) {
  return (
    <div className="min-h-screen flex items-center justify-center text-[var(--color-text-muted)]">
      Timeline page coming soon
    </div>
  );
}
